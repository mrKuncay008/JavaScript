// let enigma = "Enigma Bootcamp Keren";
// let kata = "Mantap!";

// let stringMethode = `Awesome is ` + enigma.slice(0,15) + ', '+ kata.toUpperCase();

// console.log(stringMethode);

// Mendeklarasikan variabel `enigma` dengan string "Enigma Bootcamp Keren"
// Mendeklarasikan variabel `kata` dengan string "Mantap!"
// Menggunakan template literal untuk membuat string baru
// `enigma.slice(0, 15)` memotong string `enigma` dari indeks 0 hingga 15, hasilnya adalah "Enigma Bootcamp"
// `kata.toUpperCase()` mengubah string `kata` menjadi huruf kapital, hasilnya adalah "MANTAP!"
// Mencetak hasil stringMethode ke terminal


/* 
let x = "Ba";
let c = 2;
var v = x.concat(c);
var v3 = x+c;

console.log(v);
console.log(`${x} ${c}`);
console.log(v3);

const sentence = 'The quick brown fox jumps over the lazy dog.';
let index = 0.5;

console.log(`An index of ${index} returns the character ${sentence.at(index)}`);
// Expected output: "An index of 5 returns the character u"

index = -4;

console.log(`An index of ${index} returns the character ${sentence.at(index)}`);
// Expected output: "An index of -4 returns the character d"
*/

// backticks atau template literals.
let x = `saya ini masseh pacarnya nya aisma meo
tapi yaa kek gitu lah yowesss \n`;

console.log(x);

console.log(`Backticks digunakan dalam kode JavaScript Anda karena mereka memungkinkan penggunaan template literals yang
menyediakan fitur interpolasi variabel, pembuatan string multi-baris, dan pengelolaan string yang lebih fleksibel
dan mudah dibaca dibandingkan dengan tanda kutip tunggal (') atau ganda ("). Dengan template literals, Anda dapat
menyisipkan variabel dan ekspresi secara langsung ke dalam string tanpa harus menggabungkan string secara manual 
atau menggunakan karakter newline.`);


