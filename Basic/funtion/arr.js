function arrObjt(Bar) {
    var hasil = [];

    for (let i = 0; i < Bar.length; i++) {
        C
        obj.Tipe = Bar[i][1];
        obj.Nama = Bar[i][0];
        obj.Harga = Bar[i][2];
        hasil.push(Bar);
    }
    return hasil;
}
var Bar = [['Montong','Kuncay','90000']];
console.log(arrObjt[Bar]);